# =============================================================================
# backtest - 環境変数
# =============================================================================
# バックテスト固有の設定です。
# グローバル変数（DATABASE_URL等）は root/.env で定義されています。
# =============================================================================

# ログ出力レベル: ERROR | WARN | LOG | INFO | DEBUG
LOG_LEVEL=INFO

# 取引所識別子
EXCHANGE=extended

# 取引対象シンボル
SYMBOL=BTC-USD

# tick 間隔（ミリ秒）
TICK_INTERVAL_MS=200
